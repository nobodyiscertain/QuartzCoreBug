Getting this error when running Rake:

Objective-C stub for message `functionWithControlPoints::::` type
`@@:ffff` not precompiled. Make sure you properly link with the
framework or library that defines this message.
